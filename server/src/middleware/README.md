# Shared Middleware

This directory is for central authentication, role authorization, validation, upload validation, not-found handling and error handling.

Do not create separate authentication or error-handling systems inside individual modules.
