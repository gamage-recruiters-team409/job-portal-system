# Frontend Constants

Use the approved shared status values from `statuses.js` rather than typing status strings inside components.

Any change to a shared role or status requires Team Lead approval and must be coordinated with the matching backend constants.
