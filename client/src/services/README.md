# Frontend Services

Use `apiClient.js` as the single shared Axios instance.

- Do not create a second global Axios instance.
- Keep module-specific API functions in clearly named service files.
- Do not hard-code backend URLs.
- Coordinate request and response changes with the related API owner and Team Lead.
